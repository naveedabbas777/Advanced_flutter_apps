import 'package:flutter/material.dart';

class ExportPrintUdharScreen extends StatelessWidget {
  const ExportPrintUdharScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Export/Print Udhar')),
      body: const Center(child: Text('Export/Print Udhar Placeholder')),
    );
  }
}
